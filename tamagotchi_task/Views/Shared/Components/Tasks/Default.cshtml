@model IQueryable<CharacterTask>

<div class="wrapper">
    <div id="rectangle3"></div>
    <div class="btn-group btn-group-vertical" data-toggle="buttons">
        <div class="container">
            <input type="text" maxlength="12" placeholder="Search" class="searchbar">
            <img src="https://images-na.ssl-images-amazon.com/images/I/41gYkruZM2L.png" alt="search icon" class="buttonSearch">
        </div>
        <button onclick="dropDown()" class="btn btn-default dropdown-toggle dropbtn" type="button">
            <span class="caret"></span>
        </button>
        <div id="myDropdown" class="dropdown-content">
            <a>Name</a>
            <a>Description</a>
            <a>Date</a>
        </div>
        <label class="btn active">
        @foreach (var c in Model) {

            <form id="form-@c.Id" asp-action="Complete" asp-controller="Home">
        <input type="hidden" name="taskID" value="@c.Id">
        <input type="checkbox" unchecked onclick="document.getElementById('form-@c.Id').submit();" value="@c.Id"><i class="fa fa-square-o fa-2x"></i><i class="fa fa-check-square-o fa-2x"></i><span>@c.Name</span>
        </form>
            <span class="descrdate">
                <form id="f-@c.Id" asp-action="Delete" asp-controller="Home">
                    <input type="hidden" name="taskID" value="@c.Id">
                    <input type="image" src="~/img/trash.png" onclick="document.getElementById('f-@c.Id').submit();" title="Delete" class="delete">
                </form>
                <span class="descr"> @c.Description </span>
                <div class="descrimg">
                    <img src="~/img/strength.png">
                    <img src="~/img/strength.png">
                </div>
                <datetime class="date"> @c.DeadLine </datetime>
            </span>
        }
        </label>
</div>
</div>